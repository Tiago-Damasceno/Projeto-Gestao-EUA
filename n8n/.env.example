# Copy to .env.local and replace both placeholders with independent random values.
# Never commit .env.local.
N8N_ENCRYPTION_KEY=replace-with-a-long-random-value
N8N_USER_MANAGEMENT_JWT_SECRET=replace-with-another-long-random-value
